---
/**
 * One page. The theme sentence this is built from:
 *
 *   "Every mark is one page measured against its own quiet, laid on one axis of days since
 *    the day. This is a stopwatch, not a front page."
 *
 * What that decides, and what it throws out, is in .claude/plans/.
 */
import plate from '../gen/plate.json'
import { held } from '../lib/marks/held.js'
import { ACCENT, INK, MUTED, HAIR } from '../lib/ink.js'
import DcdMark from '../components/DcdMark.astro'
import { SITE } from '../lib/site.js'

const first = held(plate, 1180)
const w = plate.meta.window
const nf = new Intl.NumberFormat('en-US')
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Record Days</title>
    <meta name="description" content={`The ${plate.stat.all} biggest single days any English Wikipedia page has ever had, and how long the world stayed.`} />
    <link rel="canonical" href={SITE} />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="preload" as="font" type="font/woff2" crossorigin href="/fonts/martian-mono-var-latin.woff2" />
    <link rel="preload" as="font" type="font/woff2" crossorigin href="/fonts/schibsted-grotesk-var-latin.woff2" />
  </head>

  <body>
    <header class="head">
      <h1>Record&nbsp;Days</h1>
      <p class="stand">
        The <b>{plate.stat.all}</b> biggest single days any English Wikipedia page has ever
        had, and how long the world stayed.
      </p>
    </header>

    <main>
      <section id="held">
        <p class="num">01</p>
        <h2>The world looks for about a month.</h2>
        <p class="deck">
          Of the {plate.stat.all} record days, <b>{plate.stat.back}</b> came back to normal.
          The middle one took <b>{plate.stat.median}</b> days.
        </p>

        <figure class="fig" id="held-fig">
          <div class="plate-box" id="held-plate" set:html={first.svg}></div>
          <div class="tip" id="held-tip" hidden></div>
          <figcaption>
            Days from a page's record day until it is back under twice its own quiet level and
            holds there a week. Quiet level is the median of days {Math.abs(plate.meta.before[0])}
            {' '}to {Math.abs(plate.meta.before[1])} before its record day. A mark's colour is
            the axis colour where it stops, so the axis is the key.
          </figcaption>
        </figure>

        <details class="method">
          <summary>How this is measured</summary>
          <p>
            A page's <b>quiet level</b> is the median of its daily readings from day
            &minus;{Math.abs(plate.meta.before[0])} to day &minus;{Math.abs(plate.meta.before[1])}.
            The census also offers days &minus;21 to &minus;8, and this site never uses it: for
            45 rows that window is already inside the event. One definition of before, for
            every section.
          </p>
          <p>
            <b>{plate.stat.drawn}</b> of the {plate.stat.all} rows are drawn.
            <b>{plate.stat.noQuiet}</b> have no quiet window in the thirty days the file
            carries, so there is nothing to measure them against. Of the rest,
            <b>{plate.stat.back}</b> came back, <b>{plate.stat.holdout}</b> were watched a full
            year and were still above twice their own level at the end of it, and
            <b>{plate.stat.running}</b> have a record day too recent for the file to say either
            way. Those {plate.stat.holdout + plate.stat.running} run past the axis and fade.
          </p>
          <p>
            The FIFA World Cup's one day is the softest number here. A tournament that runs
            longer than a month has no quiet window inside the thirty days before its final, so
            its "quiet" level is partly the tournament itself, at
            {' '}{nf.format(plate.rows.find((r) => r.a === 'FIFA_World_Cup').peak)} readings on
            the day. The Academy Awards and WrestleMania rows sit at the same wall for the same
            reason.
          </p>
          <p>
            All {plate.stat.all} rows were selected for having spiked and then fallen by day
            seven, so "they rise and fall" is the definition of this set and is not a finding
            in it. Nothing here rests on that shape: every number above is read at day
            &minus;1 or after day 180, which the selection never looks at.
          </p>
          <p>
            Below about 640 pixels wide the shortest marks are drawn at a floor of 1.2 pixels,
            so one day and two days are not separable there. Every other mark on the plate is
            its true length.
          </p>
        </details>

        <p class="src">
          Wikimedia daily pageviews, en.wikipedia, all-access, user agents only.
          {' '}{w.start} to {w.end}. Public domain, CC0.
        </p>
      </section>
    </main>

    <DcdMark />

    <style define:vars={{ accent: ACCENT, ink: INK, muted: MUTED, hair: HAIR }}>
      :root { color-scheme: light; }
      html { -webkit-text-size-adjust: 100%; }
      body {
        margin: 0;
        background: #fff;
        color: var(--ink);
        font-family: 'Schibsted Grotesk', -apple-system, 'Segoe UI', Helvetica, Arial, sans-serif;
        font-size: 16px;
        line-height: 1.5;
        -webkit-font-smoothing: antialiased;
      }
      .head, main, .src { max-width: 1240px; margin: 0 auto; }
      .head { padding: 56px 24px 8px; }
      h1 {
        font-family: 'Martian Mono', ui-monospace, monospace;
        font-weight: 600;
        font-size: 26px;
        letter-spacing: -0.02em;
        margin: 0;
      }
      .stand { margin: 10px 0 0; max-width: 46ch; color: var(--muted); font-size: 16px; }
      .stand b { color: var(--ink); font-weight: 500; }

      section { padding: 44px 24px 8px; }
      .num {
        font-family: 'Martian Mono', ui-monospace, monospace;
        font-size: 14px; font-weight: 600; letter-spacing: 0.06em;
        color: var(--accent); margin: 0 0 10px;
      }
      h2 { font-size: 30px; font-weight: 500; letter-spacing: -0.02em; margin: 0; max-width: 36ch; }
      .deck { margin: 10px 0 26px; max-width: 52ch; color: var(--muted); font-size: 17px; }
      .deck b { color: var(--ink); font-weight: 500; font-family: 'Martian Mono', ui-monospace, monospace; font-size: 15px; }

      .fig { margin: 0; position: relative; }
      .plate-box { width: 100%; }
      .plate { display: block; width: 100%; height: auto; }
      figcaption {
        margin: 16px 0 0; max-width: 62ch; color: var(--muted); font-size: 14px; line-height: 1.45;
      }

      .method { margin: 26px 0 0; border-top: 1px solid var(--hair); max-width: 68ch; }
      .method summary {
        cursor: pointer; padding: 12px 0; font-size: 14px; color: var(--muted);
        list-style: none;
      }
      .method summary::-webkit-details-marker { display: none; }
      .method summary::before { content: '+ '; color: var(--accent); font-weight: 600; }
      .method[open] summary::before { content: '\2212 '; }
      .method summary:focus-visible { outline: 2px solid var(--accent); outline-offset: 3px; }
      .method p { margin: 0 0 12px; font-size: 14px; line-height: 1.55; color: var(--muted); }
      .method b { color: var(--ink); font-weight: 500; font-family: 'Martian Mono', ui-monospace, monospace; }

      .src { padding: 20px 24px 44px; font-size: 14px; color: var(--muted); }

      .tip {
        position: absolute; z-index: 3; pointer-events: none;
        background: #fff; border: 1px solid var(--hair);
        padding: 6px 9px; font-size: 14px; line-height: 1.25; white-space: nowrap;
        box-shadow: 0 2px 10px rgba(22, 24, 29, 0.10);
      }

      @media (max-width: 639px) {
        .head { padding: 34px 16px 4px; }
        h1 { font-size: 21px; }
        .stand { font-size: 15px; }
        section { padding: 30px 16px 6px; }
        h2 { font-size: 23px; }
        .deck { font-size: 16px; margin-bottom: 20px; }
        .deck b { font-size: 14px; }
        .src { padding: 16px 16px 34px; font-size: 13px; }
        figcaption, .method p, .method summary, .tip { font-size: 13px; }
      }
      @media (prefers-reduced-motion: reduce) { * { transition: none !important; } }
    </style>

    <style is:global>
      @font-face {
        font-family: 'Martian Mono'; font-style: normal; font-weight: 400 700; font-display: swap;
        src: url('/fonts/martian-mono-var-latin.woff2') format('woff2');
        unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+0304, U+0308, U+0329, U+2000-206F, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
      }
      @font-face {
        font-family: 'Martian Mono'; font-style: normal; font-weight: 400 700; font-display: swap;
        src: url('/fonts/martian-mono-var-latin-ext.woff2') format('woff2');
        unicode-range: U+0100-02BA, U+02BD-02C5, U+02C7-02CC, U+02CE-02D7, U+02DD-02FF, U+1E00-1E9F, U+1EF2-1EFF, U+2C60-2C7F, U+A720-A7FF;
      }
      @font-face {
        font-family: 'Schibsted Grotesk'; font-style: normal; font-weight: 400 700; font-display: swap;
        src: url('/fonts/schibsted-grotesk-var-latin.woff2') format('woff2');
        unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+0304, U+0308, U+0329, U+2000-206F, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
      }
      @font-face {
        font-family: 'Schibsted Grotesk'; font-style: normal; font-weight: 400 700; font-display: swap;
        src: url('/fonts/schibsted-grotesk-var-latin-ext.woff2') format('woff2');
        unicode-range: U+0100-02BA, U+02BD-02C5, U+02C7-02CC, U+02CE-02D7, U+02DD-02FF, U+1E00-1E9F, U+1EF2-1EFF, U+2C60-2C7F, U+A720-A7FF;
      }
      @font-face {
        font-family: 'Archivo'; font-style: normal; font-weight: 600; font-display: swap;
        src: url('/fonts/archivo-latin-600-normal.woff2') format('woff2');
      }

      /* The plate and the readout are injected as markup, so their type is styled globally
         rather than scoped: Astro's scope attribute never reaches a node made by innerHTML. */
      .tip b { font-weight: 500; }
      .tip i {
        font-style: normal; font-family: 'Martian Mono', ui-monospace, monospace;
        font-size: 14px; color: #B31E63;
      }
      @media (max-width: 639px) { .tip i { font-size: 13px; } }
      /* The plate is injected as markup, so its type is styled globally rather than scoped. */
      .plate text {
        font-family: 'Schibsted Grotesk', -apple-system, 'Segoe UI', Helvetica, Arial, sans-serif;
        paint-order: stroke fill;
        stroke: #fff;
        stroke-width: 3px;
        stroke-linejoin: round;
      }
      .plate .m-name { font-size: 14px; font-weight: 500; }
      .plate .m-fig,
      .plate .m-tick,
      .plate .m-note { font-family: 'Martian Mono', ui-monospace, monospace; font-size: 14px; }
      .plate .m-note { font-weight: 600; }
      .plate .m-band { font-size: 14px; }
      .plate .m-rows path { transition: opacity 120ms linear; }
      .plate.lit .m-rows path { opacity: 0.42; }
      .plate.lit .m-rows path.on { opacity: 1; }

      @media (max-width: 639px) {
        .plate .m-name,
        .plate .m-fig,
        .plate .m-tick,
        .plate .m-note,
        .plate .m-band { font-size: 13px; }
      }
      @media (prefers-reduced-motion: reduce) { .plate .m-rows path { transition: none; } }
    </style>

    <script>
      import plate from '../gen/plate.json'
      import { held } from '../lib/marks/held.js'

      const box = document.getElementById('held-plate')
      const fig = document.getElementById('held-fig')
      const tip = document.getElementById('held-tip')

      // Draw at the width the reader actually has. The server render is one width and any
      // other width would be that drawing stretched, which moves every label off its gate size.
      let last = 0
      const draw = () => {
        const w = Math.round(box.clientWidth)
        if (!w || w === last) return
        last = w
        box.innerHTML = held(plate, w).svg
      }
      draw()
      // The box, not the window: a phone's URL bar resizes the viewport without resizing this.
      new ResizeObserver(draw).observe(box)

      const clear = () => {
        const svg = box.querySelector('svg')
        if (svg) { svg.classList.remove('lit'); svg.querySelectorAll('.on').forEach((n) => n.classList.remove('on')) }
        tip.hidden = true
      }

      box.addEventListener('pointermove', (e) => {
        const path = e.target.closest('.m-rows path')
        const svg = box.querySelector('svg')
        if (!path || !svg) return clear()
        svg.classList.add('lit')
        svg.querySelectorAll('.on').forEach((n) => n.classList.remove('on'))
        path.classList.add('on')
        tip.innerHTML = '<b>' + path.dataset.t + '</b> &nbsp;<i>' + path.dataset.v + '</i>'
        tip.hidden = false
        const r = fig.getBoundingClientRect()
        const tw = tip.offsetWidth
        tip.style.left = Math.max(0, Math.min(r.width - tw, e.clientX - r.left + 14)) + 'px'
        tip.style.top = (e.clientY - r.top - 34) + 'px'
      })
      box.addEventListener('pointerleave', clear)
      // On touch there is no pointerleave. A scroll that starts on the plate ends as a cancel.
      box.addEventListener('pointercancel', clear)
      window.addEventListener('scroll', clear, { passive: true })
    </script>
  </body>
</html>
